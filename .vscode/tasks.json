{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Compilar y Ejecutar C (UCRT64)",
            "command": "gcc ${fileBasename} -o ${fileBasenameNoExtension}.exe && ./${fileBasenameNoExtension}.exe",
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compila y ejecuta el archivo activo en su propia carpeta de forma segura."
        }
    ]
}
